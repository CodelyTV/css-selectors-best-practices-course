{
  "name": "css-selectors-course",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "sass scss:css",
    "watch": "sass scss:css --watch",
    "server": "live-server --host=localhost --port=3000",
    "dev": "run-p watch server"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "npm-run-all": "^4.1.5",
    "sass": "^1.57.1",
    "stylelint-config-rational-order": "^0.1.2",
    "stylelint-config-standard-scss": "^7.0.1",
    "stylelint-order": "^6.0.2"
  }
}
